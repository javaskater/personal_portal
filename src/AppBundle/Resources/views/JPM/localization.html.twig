{# From https://twig.symfony.com/doc/2.x/tags/import.html #}
{# Using https://twig.symfony.com/doc/2.x/templates.html#whitespace-control to remove whitespaces from the return !!! #}
{% macro localized_include(twig_file_prefix, locale, params) %}
   {% set twig_file_full_path=twig_file_prefix~"_"~locale~".html.twig" %}
   {{ include(twig_file_full_path, params) }}
{% endmacro %}